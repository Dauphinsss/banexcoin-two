/**
 * Strings que representan valores decimales financieros.
 * Nunca usar `number` para dinero — ver CONVENTIONS.md sección 1.
 */
export type DecimalString = string

export type Currency = 'BOB' | 'USDT'
